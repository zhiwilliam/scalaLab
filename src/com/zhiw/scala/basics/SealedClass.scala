package com.zhiw.scala.oop

sealed abstract class SealedClass {
    // The difference is that all subclasses of a sealed class 
    // (whether it's abstract or not) must be in the same file as the sealed class.
}